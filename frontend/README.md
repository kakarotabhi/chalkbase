# Chalkbase frontend

Angular 22 app. See:

- [`AGENTS.md`](AGENTS.md) — conventions for this folder
- [`../docs/development/README.md`](../docs/development/README.md) — setup and workflow

```bash
npm start                  # http://localhost:4200, proxies /api → :8080
npm test -- --watch=false
npm run build
```
