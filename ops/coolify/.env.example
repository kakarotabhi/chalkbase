# Copy into the Coolify application environment. Never commit real values.
SPRING_PROFILES_ACTIVE=prod

SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/chalkbase
SPRING_DATASOURCE_USERNAME=chalkbase
SPRING_DATASOURCE_PASSWORD=

# Required on the `prod` profile: the service refuses to start without it (SetupKeyConfiguration).
# POST /api/schools creates a PostgreSQL schema and is still permitAll — this shared header is what
# stands in front of it until platform-operator accounts exist. Generate with
# `openssl rand -base64 32`, and send it as X-Chalkbase-Setup-Key when onboarding a school.
CHALKBASE_SETUP_KEY=

# Required on the `prod` profile: the service refuses to start without it (EncryptionKeyConfiguration).
# Every Restricted column (ADR-0022) is encrypted under this key. Generate a DIFFERENT 256-bit key
# from CHALKBASE_SETUP_KEY above with `openssl rand -base64 32` — the two must never be the same
# value. There is NO recovery from losing this key: back it up somewhere that is not this VPS before
# a real school's data depends on it. See docs/operations/encryption-key.md.
CHALKBASE_ENCRYPTION_KEY=

# Trim actuator exposure in production.
MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE=health,info

# SMS / WhatsApp / email providers — fill in when the communication module ships.
# CHALKBASE_SMS_PROVIDER=
# CHALKBASE_SMS_API_KEY=
