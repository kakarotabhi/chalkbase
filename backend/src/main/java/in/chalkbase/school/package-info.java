/**
 * School (tenant) registry: the schools and branches Chalkbase serves, and their board/affiliation
 * details. Every other module scopes its data to a school from here.
 */
@org.springframework.modulith.ApplicationModule(displayName = "School")
package in.chalkbase.school;
