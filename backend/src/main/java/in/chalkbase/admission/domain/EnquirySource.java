package in.chalkbase.admission.domain;

/** Where a prospective family's first contact came from (FR-016). */
public enum EnquirySource {
    WALK_IN,
    PHONE,
    WEBSITE,
    REFERRAL,
    CAMPAIGN,
    IMPORTED
}
